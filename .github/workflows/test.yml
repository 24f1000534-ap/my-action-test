name: My First Action

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24f1000534@ds.study.iitm.ac.in
        run: echo "Hello, world!"
